FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y \
    gcc \
    python3-dev \
    build-essential \
    && rm -rf /var/lib/apt/lists/*
COPY bot_requirements.txt .
RUN pip install --no-cache-dir -r bot_requirements.txt
COPY . .
CMD ["python", "CookWizardBot.py"]